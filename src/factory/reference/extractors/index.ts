/**
 * Future extractor plug-in seam.
 *
 * Phase B starts with a manual seed registry so the product can use reference
 * harnesses immediately. Later extractors should emit the same
 * ReferencePatternRecord shape from repo scans, manifests, hooks, and skills.
 */
export const REFERENCE_EXTRACTOR_CONTRACT_VERSION = '0.1.0';
